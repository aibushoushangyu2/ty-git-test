①node.js特点：
单线程
非阻塞I/O	
事件驱动
②
Ryan Dahl

09年5月发布

单线程
Node可以在不新增额外线程的情况下，可以对任务进行并发处理 


Node 是一个让 JavaScript 运行在服务端的开发平台，基于V8引擎。


一。IO请求的两个阶段：
1.等待资源阶段：IO请求一般需要请求特殊的资源（如磁盘、RAM、文件），当资源被上一个使用者使用没有被释放时，IO请求就会被阻塞，直到能够使用这个资源。
2.使用资源阶段：真正进行数据接收和发生。

二。在等待数据阶段，IO分为阻塞IO和非阻塞IO。

1.阻塞IO： 资源不可用时，IO请求一直阻塞，直到反馈结果（有数据或超时）。

2.非阻塞IO：资源不可用时，IO请求离开返回，返回数据标识资源不可用



三。在使用资源阶段，IO分为同步IO和异步IO。

1.同步IO：应用阻塞在发送或接收数据的状态，直到数据成功传输或返回失败。

2.异步IO：应用发送或接收数据后立刻返回，数据写入OS缓存，由OS完成数据发送或接收，并返回成功或失败的信息给应用。



事件驱动：
在node中，在一个时刻，只能执行一个事件回调函数，但是在执行一个回调函数中推可以转而处理其他事件，也称为事件环

底层是C++编写



node.js适合用来开发什么应用程序？

应用程序需要处理大量并发的I/o，而在向客户端发出响应之前，应用程序内部并不需要进行非常复杂的处理的时候可以用node.js。 也适合于web socket配合，开发长连接的实时交互应用程序。

例子：
用户表单收集
考试系统
聊天室
图文直播
原生api（提供json的api）

node -v查询node的版本号

cd进入目录 cls清除命令行内容


在浏览器访问的时候输入localhost：3000


window.location:

href

hash #

search ?

port 端口

host 主机地址

hostname 主机名

①什么是UA？？：

User Agent 浏览器标识符

浏览器标识（UA）可以使得服务器能够识别客户使用的操作系统及版本、CPU 类型、浏览器及版本、浏览器渲染引擎、浏览器语言、浏览器插件，从而判断用户是使用电脑浏览还是手机浏览，让网页作出自动的适应。

例如UC里面的选择浏览器版本ios，安卓或者电脑端

②什么是amd和cmd的区别？

命令提示符

这些规范的目的都是为了 JavaScript 的模块化开发，特别是在浏览器端的。
这些规范都能达成浏览器端模块化开发的目的。

区别：

1. 对于依赖的模块，AMD 是提前执行，CMD 是延迟执行。不过 RequireJS 从 2.0 开始，也改成可以延迟执行（根据写法不同，处理方式不同）。CMD 推崇 as lazy as possible.

2. CMD 推崇依赖就近，AMD 推崇依赖前置。






http 服务器模块

fs文件系统模块

createServer（）.listen（）创建与监听

readFile（）读文件  第一个参数为文件地址，第二个为回调函数（两个参数err，data）


文本类型=》text/html text/css image
